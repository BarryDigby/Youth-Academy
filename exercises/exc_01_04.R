# Delete the comment "#" before the print statement, then run the code block. 
# Do this for both print statements. 

# Lets see what happens mixing data types in a vector. 
i <- 12

# i holds a numeric, and 55 is itself numeric.
tester <- c(i, 55) 
#print(tester)
#typeof(tester)

# let's add a character string.
tester_1 <- c(55, i, "epsilon") 
#print(tester_1)
#typeof(tester_1)